# Используйте базовый образ с Python
FROM python:3.12

# Установите рабочую директорию
WORKDIR /app

# Скопируйте файл зависимостей
COPY requirements.txt .

# Установите виртуальное окружение
RUN python -m venv /venv

# Установите зависимости в виртуальное окружение
RUN /venv/bin/pip install --no-cache-dir -r requirements.txt

# Скопируйте весь проект в контейнер
COPY . /app/

# Установите переменную окружения, чтобы указывать на виртуальное окружение
ENV PATH="/venv/bin:$PATH"

# Команда по умолчанию для запуска приложения (с использованием виртуального окружения)
CMD ["python", "manage.py", "runserver", "0.0.0.0:8000"]
